# 交接：2026-08-28 停机状态

新会话从这里开始。读完这一份 + [`AGENTS.md`](./AGENTS.md) 就能接手。
上一份停机状态见 [`HANDOFF-2026-08-08.md`](./HANDOFF-2026-08-08.md)（其中的待办 P0/P1/P2 本轮已全部处理，见下）。

## 一句话现状

`video-distill` 的四个已知缺口（P0 措辞矛盾、P1 触发数据、P2 盲测、两份存量笔记质量）
本轮全部关闭并留下实测记录；两份存量笔记（C05CGMj_LVg、LNH4I47ufLk）都通过了
「盲测 → 修复 → 复测通过」的完整闭环。**流程文档（SKILL.md）现在与实测一致。**

## 一、本轮做完的事（每条带判据）

### P0 · 「唯一索引写入点」矛盾 —— 已修（源码 + 实测）

- 源码核对：`surfaces/cli/main.py` 只在 `if index and result.perception is not None`
  时才调 `index_watch_result`；`--transcript-only` 恒无 perception
  ⇒ **这条路径上 `--index/--no-index` 开关是死的，加不加都不写索引**。
- SKILL.md 阶段 1 改为按内容类型二选一：操作型/界面密集走带索引 watch
  （转录+索引一次拿到，阶段 2.1 语义定 cue 可用）；口播型保留 `--transcript-only`
  快路径并明示不写索引。阶段 2.1 加了「索引在不在」的前置判断与补跑指引。
- references 第 1 节、AGENTS.md 3.1 同步修正。
- 实测支撑：真实索引上中文词组命中 0.82–0.88、英文查询命中 OCR 行 0.71+
  （`fastembed` 已装，语义检索确认可用 —— 顺手关闭了旧 HANDOFF「尚未验证」第 3 条）。

### P1 · Claude 侧 20 条触发对比 —— 已跑完（sonnet）

结果：**9/9 命中，0 真实误触发**，1 条负例超时不计入。
数据与途中两个仪器事故（429 假数据、汇总把超时算误触发）见
[`docs/experiments/trigger-claude-2026-08-28.md`](./docs/experiments/trigger-claude-2026-08-28.md)。
`trigger_test.py` 新增 `--model` 与 429 检测。

### P2 · 第三次盲测 —— 已做，且多做了一轮

对 C05CGMj_LVg：第三测确认顺序硬停仍在（交叉引用只让它「可发现」，没消除），
随即**根治**（步骤 11/12/13 按 transcript 实证的时间线重排，`simple` 无来源参数删除，
`0.9 0.9` 补 [25:29] 锚，xformers 补 torch 副作用警示，`dynamic memory` 日志行
标注「官方文档补全、未逐字核对」）；第四测确认**硬停消失**（1–13 线性可走）。
validate 从 0 错 1 提醒 → **0 错 0 提醒**。

### 存量笔记 LNH4I47ufLk —— 两轮盲测闭环（本轮新增的 E2E 演练）

8/2 的旧产出（validate 1 错 1 提醒、从未盲测过）：

- 首测（沿 R1→R2）：R1 段、步骤 1/2/6 被判「完全可执行」；抓出时长/fps/frame_count
  与验证 4 顶牛、闸门 0 对纯 R1 不自洽、症状表 R4 张冠李戴、跨 Scene 背景歧义等。
- 修复 8 处 + transcript 复核（视频只演示一个 Scene、下载入口从未演示 ⇒ 诚实标注）。
- 复测结论：**「能」走通到「点运行、队列跑完、看到成片」**，剩余软点全部被手册显式承认。

### validate.py —— 修了两个真 bug（都带实测）

1. **假「重叠」**：旧判据在起点倒退时把「上一步终点 − 本步起点」当重叠量，
   两个完全不相交的区间（`[02:43–03:04]` 排在 `[08:01–09:08]` 后）被报成
   「重叠 385s」。改为**真区间相交**（min 终点 − max 起点 > 0）。
2. **路线组织的合法乱序被误杀**：新增 frontmatter `organization: task-routes`
   声明，声明后「步骤时间戳单调」从 ERROR 降为提醒（时间戳只是回查凭据）；
   未声明仍是 ERROR，判据不放松。
   双向复验：C05 仍 0/0，未声明时 LNH4 仍报 ERROR（消息里指引如何声明）。

## 二、尚未验证（不要当成验过）

1. **修订后的 SKILL.md 阶段 1 路径 B（带索引 watch）没有完整新跑过一遍**——
   索引可用性由现存索引 + search 实测支撑，但「从零对一新视频走路径 B」未跑。
2. `large-v3-turbo` 在 CPU 上仍未测（沿旧 HANDOFF）。
3. trigger 测试用的是 **sonnet**（默认模型月度限额 429）；默认模型下的触发行为
   未测。早上的单条最小调用成功**不**证明配额可用（当天稍后即 429）。
4. C05CGMj_LVg 的 workflow 文件名、LNH4I47ufLk 的下载入口/上传节点名——
   **素材边界**（画面里没有），维持「不要猜」标注，别再试图重抽。
5. 213（Fedora）上的复现仍停留在 8/8 状态，本轮改动未在那台机器验证。

## 三、下一步建议（按价值）

1. 挑一个**新的**短教学视频，从阶段 0 完整走一遍修订后的流程
   （重点验证阶段 1 路径 B + 2.1 语义定 cue 的衔接）。
2. 把 LNH4/C05 两轮「盲测 → 修复」中**仍未落成机械检查**的发现过一遍：
   「同一参数在两处给出互斥值」这类矛盾也许可以半机械化（grep 参数名）。
3. claude CLI 月度限额刷新后，用默认模型补测触发（验证模型变量影响）。

## 四、环境速查（增量）

- `claude` CLI 已升 **2.1.247**；默认模型月度限额会 429，`--model haiku/sonnet` 可用。
- `trigger_test.py` 现在必须带 `--model` 跑（除非限额恢复）。
- 中间产物照旧**不许放 /tmp**（本轮全部落在 `video-distill-workspace/`）。
