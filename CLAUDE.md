# CLAUDE.md

**这个文件刻意不含任何规则。** 全部工作约定在 [`AGENTS.md`](./AGENTS.md) ——
先读那一份，它是唯一真源。

## 为什么不在这里再写一遍

同一条规则存在两个副本，副本一定会漂移，而漂移的那一刻没人会知道。

2026-08-08 在**这个项目**里踩过这个坑的具体形态：`SKILL.md` 的质量控制那节写着
「可复现盲测：**见阶段 2.4**」，而 SKILL.md 里**从来没有 2.4 这一节**
（只有 2.1 / 2.2 / 2.3 / 2.5）——
**一个被列为必做、却从未被定义过的步骤，在文档里安静地躺了很久。**

同一天还有一个更贵的：`AGENT-START.md` 教远程 agent 跑 `watch-skill doctor`，
**却从没说这个命令从哪来**；而 `README.md` 的安装步骤依赖 `vendor/` 目录里的补丁，
**那个目录正是被 `.gitignore` 排除掉的**。两条都是「文档指向一个不存在的东西」。

> **文档里一条跑不通的指令，比没有文档更糟。**

`AGENTS.md` 每条约束都带实测依据与日期。把它抄一份到这里，只会制造同一种故障。

## 新会话的读法

1. [`AGENTS.md`](./AGENTS.md) —— 工作约定、已验证事实、证据纪律、会说谎的仪器清单
2. [`HANDOFF-2026-08-08.md`](./HANDOFF-2026-08-08.md) —— **停机状态、
   哪些验过哪些没验、下一步顺序、待修清单**
3. [`.claude/skills/video-distill/SKILL.md`](./.claude/skills/video-distill/SKILL.md)
   —— 流程真源，不要凭别的文档猜它怎么工作
4. [`HERMES-REAL-RUN.md`](./HERMES-REAL-RUN.md) —— 真跑一个 29 分钟教程的六轮记录
5. `references/engine-internals.md` —— 改任何一条流程规则之前先读它

`AGENTS.md` 里的「四档用词」（已验证 / 观察到 / 推断 / 尚未验证）和
「先找仪器，再问这个仪器看得见我要找的东西吗」，是这个项目最重要的两条规矩。

## 一句话现状

`video-distill` 这个 skill 已公开，别的机器上的 agent 能自己装上并真产出笔记
（Fedora 机器实测装成、Hermes 实测 4 分 15 秒出四件套、`validate.py` 满分）。
**但「满分」不等于「可用」** —— 盲测把一份满分手册判为「不能独立完成」，
而它一条关键命令把版本约束抄反了。
校验器已补三个洞，盲测已做成脚本；**Claude 侧 20 条触发对比跑到一半丢了数据，
是当前最大的空白。** 详见 `HANDOFF-2026-08-08.md`。
