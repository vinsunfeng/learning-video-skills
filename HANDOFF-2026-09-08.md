# 交接：2026-09-08 停机状态

新会话从这里开始。读完这一份 + [`AGENTS.md`](./AGENTS.md) 就能接手。
上一份 [`HANDOFF-2026-08-28.md`](./HANDOFF-2026-08-28.md) ——其「下一步建议」三条
本轮全部处理，见下；其中的开放项有两条随之关闭，其余仍然开放（见二）。

## 一句话现状

「视频 → 知识资产 → skill」全链路各段都有至少一次实测：修订版 SKILL.md 在
**两个 agent** 上各通过端到端（claude 侧路径 B 本轮闭环）；阶段 4 两条蒸馏路线
各有一次实测（操作型 → PLAYBOOK 已有存量；方法论型 → cangjie-skill 首跑闭环）；
hermes 视觉配通（deepseek-v4-flash-vision-exp）。仓库 README/简介/topics 已更新。

## 一、本轮做完的事（记录都在 `docs/experiments/`）

1. **claude 侧路径 B 端到端**（关闭 08-28 遗留开放项）：新视频 12.9 分钟从阶段 0
   到盲测闭环，语义定 cue 中 0.82–0.95 / 英文命中 OCR 行；`validate` 0/0。
   → `claude-pathb-2026-09-08.md`
2. **cangjie-skill 首次实测**：v2.5.0 六阶段对真实 transcript 一次跑通到编译发布；
   三重验证 21→4（19%）；两个上游 bug 本地修复（`local-patches` 4f689fa）并报
   上游 issue [#29](https://github.com/kangarooking/cangjie-skill/issues/29) /
   [#30](https://github.com/kangarooking/cangjie-skill/issues/30)。
   → `cangjie-stage4-2026-09-08.md`
3. **hermes 新 profile 端到端 + 视觉配通**：`video` profile 从创建到装配双判据通过
   （.env 凭证不继承——新发现，已补 HERMES-INSTALL §〇）；无字幕 360p 源全流程
   约 30 分钟，validate 0/0；`auxiliary.vision` 配 deepseek 视觉后双仪器检查通过。
   它自主写入 SKILL.md 的四条低清源纪律经逐条复核后收编。
   → `hermes-video-profile-2026-09-08.md`
4. **交接协议固化**：SKILL.md 阶段 4 从两行描述升级为实测契约（转录进 cangjie
   笔记不进等五条）；视觉对照表改为配置感知；`trigger_test.py` 默认输出路径
   移出 /tmp（§2.5 自我违反）；README/仓库简介/topics 重写。

## 二、尚未验证（不要当成验过）

1. **编译产出的 skill 装机后的触发测试**——链路唯一未闭环的一环
   （`trigger_test.py` 双对照纪律现成）。
2. **操作型 PLAYBOOK → 项目内 skill 的固化路线**——SKILL.md 阶段 4 标注
   「尚无实测记录」，首个试点可用任一现有 PLAYBOOK。
3. `large-v3-turbo` 在 CPU 上仍未测（沿 08-28）。
4. 默认模型的触发行为未测（08-28 第 3 条，claude 月度限额未刷新）。
5. 213（Fedora）复现仍停留在 8/8 状态。
6. ~~视觉配通后的**完整蒸馏**还没跑过~~（现有视觉证据是两张已知帧的仪器检查；
   下次 hermes 跑视频时应走双路并对比）。**2026-09-09 补**：文档基线安装路径已
   在隔离环境从零真跑验证到 OCR 出活（watch-skill 1.4.3），
   见 `docs/experiments/fresh-install-2026-09-09.md`——「新机装机没跑过」缺口
   在本机可验证范围内闭合；Apple 补丁路径与 Linux 特有环节边界见该记录。
7. 环境副作用仍在：全局 `~/.config/yt-dlp/config`（android client，仅 360p）
   影响所有 yt-dlp 调用，撤销方式在 08-28 实验记录里。
8. cangjie `local-patches` 与上游的关系待上游回应 issue 后处理（rebase 或退役）。

## 三、下一步建议（按价值）

1. 用 hermes(video) 跑一个新视频（视觉+OCR 双路），对比纯 OCR 轮的产物质量。
2. 挑一份现有 PLAYBOOK 走「固化为项目内 skill」+ 触发测试，关闭缺口 1+2。
3. claude 限额刷新后补默认模型触发测试（缺口 4）。
