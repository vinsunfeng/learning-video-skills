# Claude 侧触发准确性 · 2026-08-28 重跑

HANDOFF-2026-08-08 P1 的重跑。上一轮（2026-08-08）跑到 1/22 时 `/tmp` 被清空数据全丢。
本轮结果落在本目录（`trigger-claude.jsonl` 逐条 + `trigger-claude.log` 全程日志）。

## 结果（模型 sonnet，CLI 2.1.247）

| 项 | 结果 |
|---|---|
| 已知对照 | 正对照触发 ✓、负对照忍住 ✓（对照通过，数字才有意义） |
| 该触发 9 条 | **9/9 命中** |
| 不该触发 11 条 | **10 条忍住，1 条超时**（`__TIMEOUT__`，读不出结论，不计入任何一边） |
| 真实误触发 | **0** |

与 Hermes 侧（9/9 命中、10/11 忍住、**1 条真误触发**）对照：
Claude 侧的 description 排除清单在 20 条样例上零误触发，
「3/3 误触发是 Hermes 特有的」（8225646 的结论）在完整样本上成立。

## 途中撞上的两个仪器问题（都修在 `trigger_test.py`）

1. **月度限额 429 造出 22/22 全 False 的假数据。** 默认模型当月消费限额用尽后，
   CLI 每条查询都返回 `api_error_status: 429`，全程没有任何 `tool_use`
   ⇒ 22 条全部「无 Skill 事件」：正例 9 条全「漏」、负例 11 条全「过」。
   **正对照当场抓住了它**（对照纪律第 N 次救场），但注意负例的假通过差点混过去。
   修复：① `--model` 参数（实测 `haiku` / `sonnet` 不受该限额影响）；②
   流里见到 429 / spend limit 显式返回 `None`（没测到 ≠ 没触发）。
2. **汇总把「读不出」计入「误触发」。** 超时那条（i=14，agent 埋头真去翻译字幕到 240s）
   `got=None`，旧汇总 `len(neg) - hold` 把它算成「误触发 1」——分数会骗人：
   超时是没有结论，不是没忍住。已改为只数 `ok is False`。

## 附带观察

- 早上的「最小真调用」（一个字回答）成功**不能**证明计费通路健康——
  它与 429 之间只隔了几十条查询。`auth status` 会谎报之外，又多一条：
  **极小调用成功 ≠ 配额可用**。判据仍是真的跑通目标负载。
- 一条正例在 sonnet 下约 60–150s 出现 Skill 事件（检测到即杀）；
  负例几乎全部跑满 240s 超时。22 条全程约 55 分钟，放后台跑。
