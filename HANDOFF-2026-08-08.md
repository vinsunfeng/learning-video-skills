# 交接：2026-08-08 停机状态

新会话从这里开始。读完这一份 + [`AGENTS.md`](./AGENTS.md) 就能接手。

## 一句话现状

`video-distill` 已公开，远程 agent 能自己装上并真产出笔记。
**但产出质量的天花板不在结构，在「有没有真读画面」** ——
一份 `validate.py` 满分的手册被盲测判为「不能独立完成」，
而它把 `"triton-windows>=3.7,<3.8"` 抄成 `"<3.7"`，约束整个反了。

---

## 一、已验证（有对照，别重新试探）

| 事 | 结果 | 判据 |
|---|---|---|
| 远程机器照文档装 | ✅ Fedora 44 / x86_64 装成 | 端到端 OCR 认出我造的 `HELLO 213 OCR` |
| Hermes 出四件套 | ✅ **4:15**（第二次 6:40） | `validate.py` 我自己重跑，0 错 0 提醒 |
| 内容是否有根 | ✅ 声称的三个坑字幕里都在 | `python_embedded` 3 处、`xformers` 3 处、`CUDA 13.0` 3 处 |
| 转录 | ✅ 能用，但看模型 | 已知台词：`base` 错 4 处、`medium` 错 1 处 |
| 校验器双向 | ✅ | 未填充模板 12 错；合格笔记 0 错 |
| 盲测灵敏度 | ✅ | **种入已知错误，它抓到了** |
| 视觉 MCP | ✅ `understand_image` 可用 | 造已知图，三行文字逐字读出、认对颜色形状 |
| Claude 触发判断 | ✅ 正例触发 video-distill、负例 0 次 Skill 事件 | 抓 `stream-json` 的 `tool_use` |

## 二、尚未验证（**这一节是重点，不要当成验过**）

1. **Claude 侧 20 条触发对比：跑到 1/22 时 `/tmp` 被清空，数据全丢。**
   现存的 Claude 侧只有 2 个手动数据点 + 2 条对照。
   Hermes 侧有完整 20 条。**两边样本量不对等，不能直接比。**
   → 重跑时**结果必须落在 `docs/experiments/`,不要放 `/tmp`**。
2. **`large-v3-turbo` 在 CPU 上没测**（只验到 `medium`）。
3. **`sentence_transformers` / `fastembed` 缺失下检索到底能不能用** —— 没查清。
   `doctor` 说 `index healthy`，而 `search` 打印「keyword-only」。
4. **官方 `run_loop` 的自动优化 description 那一环完全没跑到**（见下）。
5. **盲测第二轮的硬停有没有被真正解决** —— 我加了交叉引用，但**没有第三次盲测**。

## 三、待修清单（按价值排序）

### P0 · SKILL.md 有一句与实测不符

阶段 1 被称作「**唯一的索引写入点**」并叮嘱不要加 `--no-index`，
而实测 `--transcript-only` **压根不写索引**（`list` / `search` 都找不到该视频）。
⇒ 阶段 2.1 的「对索引做语义检索定 cue」在这条路径上无从做起。
**这条没改。** 要么改流程（先做一次带索引的 watch），要么改措辞。

### P1 · 重跑 Claude 侧 20 条对比

工具已就位：

```bash
V=.claude/skills/video-distill
python3 $V/scripts/trigger_test.py $V/evals/trigger-evals.json \
  --backend claude --out docs/experiments/trigger-claude.jsonl
```

**注意三件事**（都在 `ask_claude()` 的 docstring 里）：
- 判据必须锚在**这一次 tool_use 的 input**，不是「流里出现过这个词」——
  流里有全部可用 skill 清单，会把「写个快排」判成触发 video-distill（负对照抓到过）
- **一检测到就杀进程**，否则它会真去干活（实测一条正例跑了 26 次 Bash、下载视频）
- 负例会跑到 240s 超时 ⇒ 20 条约 50 分钟，**放后台并把日志落到仓库里**

### P2 · 第三次盲测

回补之后没再盲测。命令：

```bash
python3 $V/scripts/blind_prep.py "/Users/vdev/notes/视频笔记/AI工具/yt-C05CGMj_LVg" /tmp/blind
```

然后交给干净 subagent（禁止上网、禁止读隔离目录外文件）。
上两轮结论：第一次「靠猜走到步骤 10」→ 第二次「第 1–10 步可照做走通，
硬停缩窄成一个顺序缺陷」。**第三次要看硬停是否消失。**

### P3 · 官方 harness 的兼容问题

`skill-creator/scripts/run_loop.py` 在本机**结构性不可用**：
它造一个合成 command 放进 `.claude/commands/`，而真实触发走
`~/.claude/skills/` 里的 skill —— 两套机制。
已排除的三个原因：401（已修）、description 截断（已修）、
默认 `--timeout 30` 而实测触发要 75s+（提到 240s）——**仍 20/20 全 0**。
若要用它的自动优化能力，得改它的触发检测方式（或给它一条真 skill 的安装路径）。

---

## 四、当前那份真实笔记的状态

`/Users/vdev/notes/视频笔记/AI工具/yt-C05CGMj_LVg/`
（素材：《[AI tutorial] miniMaxH3 從零開始安裝 comyUI、Triton、Sage Attention》29:12 繁中）

- 四件套 + 5 张证据帧 + 1 张回补帧，`validate.py` **0 错 1 提醒**
- 那 1 个提醒是**真的**：步骤 12/13 时间区间重叠 124 秒
- 已修：抄反的版本约束、我自己留下的空代码块、缺失的访问地址、步骤 11 的顺序缺陷
- 仍缺（画面里拿不到，**已在笔记里标明「不要猜」**）：
  workflow 文件名 —— 标签显示 `video_minimax_h3_t...`，
  **是 ComfyUI 界面自己截断的，不是 OCR 失败** ⇒ 再怎么重抽也拿不到

---

## 五、环境速查

```bash
watch-skill   ~/.local/bin/watch-skill        # 装法见 AGENTS.md §3.1，必须带 extras
vault         ~/notes                         # 笔记根：$VAULT/视频笔记/<分类>/<slug>/
hermes        ~/.local/bin/hermes             # profile: wiki（skill 已软链进 note-taking）
claude        ~/.local/bin/claude             # 2.1.226；token 会过期，判据是真调用一次
213           ssh vinsun@192.168.3.213        # Fedora，sudo 123456；gemma n_ctx=16384 < 64K
仓库          github.com/vinsunfeng/learning-video-skills  # 公开
```

**中间产物不许放 `/tmp`** —— 本次就是这么丢掉一轮 20 条数据的。

---

## 六、这一天最值钱的几句（都是被自己坑出来的）

> **一个所有条目都返回 0（或全满分）的测试，测不出任何东西** ——
> 而负例会替它伪装成一半是好的。

> **分数会骗人，理由不会。** 8/9 和 4/11 差点让我去改被测对象，
> 而理由栏写着「匹配 youtube-content skill」—— 问句压根没点名那个 skill。

> **占位符是诚实的空白；抄错的命令是自信的错误。后者更糟。**

> **一个覆盖不全的引用块，比没有引用块更容易骗人** ——
> 它把「部分有据」包装成「整条有据」。

> **一条只在有输入时才检查的规则，等于允许「没有输入」。**
> （「引用的图片存在」在零引用时也通过 ⇒ 0 证据帧的笔记拿了满分。）

> **只在一种环境里验过的结论，写成普适的那一刻就变成了错的。**
> （「description 排除项拦不住」只在 Hermes 上成立，Claude 侧是对的。）

> **agent 的一句否认，不等于一次测量。**
> 它说「没有排除清单」，让它逐字背，它背全了。

> **我为了抓错而引入的机制，第一个抓到的是我自己的错。**
> 那说明它在工作 —— 只抓别人不抓自己的检查，通常是装饰。
