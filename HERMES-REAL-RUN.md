# Hermes 真跑一次真实教学视频：2026-08-08

**问的问题**：装了 `video-distill` 的 Hermes agent，能不能真把知识点和心得写下来？

**答案**：**能，而且比预期好** —— 但它**从没看画面**，而校验器**没能发现**这件事。

---

## 一、怎么让 agent 跑起来

| 机器 | 能不能跑 | 说明 |
|---|---|---|
| **本机 Mac** | ✅ | `~/.hermes/config.yaml` 里 `model.provider: deepseek` 已配好 |
| 213（Fedora） | ❌ | 无 cloud provider；本地 gemma **`n_ctx` 实测 = 16384**，低于 Hermes 要求的 64K |

**可用命令**（本机实测跑通全流程）：

```bash
cd ~/notes    # cwd 决定 agent 的工作目录
hermes -p wiki -z '把这个教学视频沉淀成 Obsidian 笔记，要做到不看视频、
只凭文档就能复现操作：<URL>

所有需要确认的地方你自己拍板，不要问我。做完告诉我笔记写在哪、以及你自己的心得体会。' --yolo
```

**213 要跑起来只有两条路，都没测过**：

1. 重启 llama.cpp 带 `-c 65536`（`/props` 实测 `n_ctx=16384`，
   **它没有少报** ⇒ 报错里那条「改 `model.context_length`」在这里**不适用**，
   那样改是骗 Hermes）
2. 给 213 配一个 cloud provider（`hermes setup`）

## 二、它做到了什么（每条我都独立核过，没采信它的自述）

素材：《[AI tutorial] miniMaxH3 從零開始安裝 comyUI、Triton、Sage Attention》
29:12，繁中人工字幕，JoeMultimedia。

| 项 | 结果 |
|---|---|
| 耗时 | **4 分 15 秒**（19:09:04 → 19:13:19） |
| 触发 skill | ✅ 只给 URL，没提 skill 名 |
| 目录与命名 | ✅ `AI工具/yt-C05CGMj_LVg/`，slug 规范 |
| 产出 | 主笔记 6.1K｜PLAYBOOK 8.0K｜EXTEND 4.5K｜transcript 17K |
| 内容量 | 11 条带时间戳要点、11 步操作、14 条扩展 |
| **`validate.py`** | **0 错误 0 提醒 —— 我自己重跑确认，它没虚报** |
| 内容是否有根 | ✅ 它声称的三个坑，字幕里都在：`python_embedded` 3 处、`xformers` 3 处、`CUDA 13.0` 3 处 |
| 有没有编造 | ❌ 没有。不知道的 URL 写成 `<triton-wheel-url>` **占位符** |

它自己的心得里有一条我认为是真见解：

> 「视频本身最大的价值是排坑」—— 作者踩了三个坑并当场修正，
> 这些用 `[!warning]` 标了出来，只看 PLAYBOOK 也不会重蹈覆辙。

## 三、它没做的那件事，以及**校验器为什么没发现**

**`assets/` 里 0 张证据帧。** 它整份笔记只用了字幕，**从没看过一帧画面**。
而 frontmatter 里写着 `degradations: []` —— **没有申报这个降级**。

而校验器给了 0 错误 0 提醒。为什么？

> 「引用的图片存在」这条规则，在**一张图都不引用**时当然成立。
> **一条只在有输入时才检查的规则，等于允许「没有输入」。**

后果是具体的，不是理论的：PLAYBOOK 里 Triton 的 wheel 地址只能写成占位符 ——
而 skill 里早就写着「**专名、命令、参数值以 OCR 为准**」，
因为声学和字幕解决不了同音词与 URL。**没看画面 ⇒ 手册不可复现。**

### 已修：新增「证据帧非空」检查

三个方向都验过：

| 输入 | 结果 |
|---|---|
| Hermes 那份（0 帧、操作型） | **✗ 报错** |
| 加一处图片引用（27 张帧在位） | ✓ 通过 |
| 同一份改成理论型 | **!** 只提醒，不误伤 |

## 四、顺带查出的两个引擎事实

1. **`--transcript-only` 不写索引。** 而 SKILL.md 把阶段 1 称作
   「唯一的索引写入点」并叮嘱不要加 `--no-index` ——
   实测跑完 `watch-skill list` 里没有这个视频，`search` 也搜不到。
   ⇒ 阶段 2.1 的「对索引做语义检索定 cue」**在这条路径上无从做起**。
2. **`fastembed not installed` ⇒ `search` 退化成纯关键词。**
   引擎会打印这行提示，但不报错。语义检索一直没真正启用过。

**这两条都还没修** —— 它们要动 SKILL.md 的阶段设计，不是改一行能了的事。

## 五、诚实的边界

- 只跑了**一个**视频、**一个** profile、**一次**。n=1。
- 笔记的**事实正确性**只抽查了 3 处关键词 + 命令占位符，
  **没有逐条比对 510 段字幕**。
- 「不看视频能否复现」**没做盲测**（skill 的质量控制 §2 要求派一个
  只读 PLAYBOOK 的 subagent 复述）—— 而现在已知它缺 wheel URL，
  **盲测大概会失败**，那正是下一步该做的事。
- 213 上的两条修法都**没测**。
