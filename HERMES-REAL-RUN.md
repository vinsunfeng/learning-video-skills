# Hermes 真跑一次真实教学视频：2026-08-08

**问的问题**：装了 `video-distill` 的 Hermes agent，能不能真把知识点和心得写下来？

**答案**：**能，而且比预期好** —— 但它**从没看画面**，而校验器**没能发现**这件事。

---

## 一、怎么让 agent 跑起来

| 机器 | 能不能跑 | 说明 |
|---|---|---|
| **本机 Mac** | ✅ | `~/.hermes/config.yaml` 里 `model.provider: deepseek` 已配好 |
| 213（Fedora） | ❌ | 无 cloud provider；本地 gemma **`n_ctx` 实测 = 16384**，低于 Hermes 要求的 64K |

**可用命令**（本机实测跑通全流程）：

```bash
cd ~/notes    # cwd 决定 agent 的工作目录
hermes -p wiki -z '把这个教学视频沉淀成 Obsidian 笔记，要做到不看视频、
只凭文档就能复现操作：<URL>

所有需要确认的地方你自己拍板，不要问我。做完告诉我笔记写在哪、以及你自己的心得体会。' --yolo
```

**213 要跑起来只有两条路，都没测过**：

1. 重启 llama.cpp 带 `-c 65536`（`/props` 实测 `n_ctx=16384`，
   **它没有少报** ⇒ 报错里那条「改 `model.context_length`」在这里**不适用**，
   那样改是骗 Hermes）
2. 给 213 配一个 cloud provider（`hermes setup`）

## 二、它做到了什么（每条我都独立核过，没采信它的自述）

素材：《[AI tutorial] miniMaxH3 從零開始安裝 comyUI、Triton、Sage Attention》
29:12，繁中人工字幕，JoeMultimedia。

| 项 | 结果 |
|---|---|
| 耗时 | **4 分 15 秒**（19:09:04 → 19:13:19） |
| 触发 skill | ✅ 只给 URL，没提 skill 名 |
| 目录与命名 | ✅ `AI工具/yt-C05CGMj_LVg/`，slug 规范 |
| 产出 | 主笔记 6.1K｜PLAYBOOK 8.0K｜EXTEND 4.5K｜transcript 17K |
| 内容量 | 11 条带时间戳要点、11 步操作、14 条扩展 |
| **`validate.py`** | **0 错误 0 提醒 —— 我自己重跑确认，它没虚报** |
| 内容是否有根 | ✅ 它声称的三个坑，字幕里都在：`python_embedded` 3 处、`xformers` 3 处、`CUDA 13.0` 3 处 |
| 有没有编造 | ❌ 没有。不知道的 URL 写成 `<triton-wheel-url>` **占位符** |

它自己的心得里有一条我认为是真见解：

> 「视频本身最大的价值是排坑」—— 作者踩了三个坑并当场修正，
> 这些用 `[!warning]` 标了出来，只看 PLAYBOOK 也不会重蹈覆辙。

## 三、它没做的那件事，以及**校验器为什么没发现**

**`assets/` 里 0 张证据帧。** 它整份笔记只用了字幕，**从没看过一帧画面**。
而 frontmatter 里写着 `degradations: []` —— **没有申报这个降级**。

而校验器给了 0 错误 0 提醒。为什么？

> 「引用的图片存在」这条规则，在**一张图都不引用**时当然成立。
> **一条只在有输入时才检查的规则，等于允许「没有输入」。**

后果是具体的，不是理论的：PLAYBOOK 里 Triton 的 wheel 地址只能写成占位符 ——
而 skill 里早就写着「**专名、命令、参数值以 OCR 为准**」，
因为声学和字幕解决不了同音词与 URL。**没看画面 ⇒ 手册不可复现。**

### 已修：新增「证据帧非空」检查

三个方向都验过：

| 输入 | 结果 |
|---|---|
| Hermes 那份（0 帧、操作型） | **✗ 报错** |
| 加一处图片引用（27 张帧在位） | ✓ 通过 |
| 同一份改成理论型 | **!** 只提醒，不误伤 |

## 四、顺带查出的两个引擎事实

1. **`--transcript-only` 不写索引。** 而 SKILL.md 把阶段 1 称作
   「唯一的索引写入点」并叮嘱不要加 `--no-index` ——
   实测跑完 `watch-skill list` 里没有这个视频，`search` 也搜不到。
   ⇒ 阶段 2.1 的「对索引做语义检索定 cue」**在这条路径上无从做起**。
2. **`fastembed not installed` ⇒ `search` 退化成纯关键词。**
   引擎会打印这行提示，但不报错。语义检索一直没真正启用过。

**这两条都还没修** —— 它们要动 SKILL.md 的阶段设计，不是改一行能了的事。

## 五、诚实的边界

- 只跑了**一个**视频、**一个** profile、**一次**。n=1。
- 笔记的**事实正确性**只抽查了 3 处关键词 + 命令占位符，
  **没有逐条比对 510 段字幕**。
- 「不看视频能否复现」**没做盲测**（skill 的质量控制 §2 要求派一个
  只读 PLAYBOOK 的 subagent 复述）—— 而现在已知它缺 wheel URL，
  **盲测大概会失败**，那正是下一步该做的事。
- 213 上的两条修法都**没测**。

---

# 第二次：只改一个变量（SKILL.md 的视觉对照表）

**2026-08-08 同日，同视频、同提示词，唯一变化是 skill 里加了「你可能读不了图」那张表。**

| | 第一次 | 第二次 |
|---|---|---|
| 耗时 | 4:15 | **6:40** |
| 证据帧 | **0 张** | **5 张**，命名带时间戳，PLAYBOOK 里引用 5 处 |
| `<...>` 占位符 | 1 处 | **0 处** |
| `validate.py` | 0 错 0 提醒 | 0 错 0 提醒（且这次「证据帧非空」是真过） |

**指令改了一段话，行为就变了。** 它不是没有视觉能力 ——
是没人告诉它「你不能 `Read` 图，但你有 `understand_image`」。

## 但占位符消失不等于变对了 —— 它抄错了

我把它引用的那张帧**自己放大 3.2× 读了一遍**。画面原文是：

```
pip install -U "triton-windows>=3.7,<3.8"
Collecting triton-windows<3.8,>=3.7
Downloading triton_windows-3.7.1.post27-cp313-cp313-win_amd64.whl (69.7 MB)
```

而它写进 PLAYBOOK 的是 **`-U "triton-windows<3.7"`** ——
**丢掉 `>=3.7,` 之后约束整个反了**，照着装会拿到 3.7 **以下**的旧版本。

> **占位符是诚实的空白；抄错的命令是自信的错误。后者更糟。**
> 占位符会让人去查，错命令让人在别处排错。
> 而正确答案就在**它自己引用的那张帧里**。

### 三条结论

1. **加了画面 ⇒ 占位符消失,但引入了一个更难发现的错。**
   「占位符数量」是个**会骗人的指标** —— 它降到 0 看起来像进步。
2. **`validate.py` 结构上查不出这类错。** 它能校验时间戳、hash、结构，
   **不能校验你有没有读对画面**。指望校验器兜住语义保真是指望错了地方。
3. 所以 SKILL.md 新增两条硬规则：命令**逐字复制不得重述**；
   **画面原文以引用块留在紧邻位置** ——
   把不可自动检查的东西，变成可人工核对的。

已修正该笔记，并附上画面原文引用块。

## 顺带：GitHub 那一路同日验完

| 检查 | 结果 |
|---|---|
| 六份文档匿名 raw | 全部 **200** |
| 全新匿名 `clone` | **428K / 17 文件**，skill 树完整 |
| 下载版脚本能跑 | ✅ 新增的「证据帧非空」检查在下载版里也生效 |
| SKILL.md 视觉对照表 | ✅ 已在线 |

**而正是这次「从 GitHub 装完真跑」抓出一个崩溃**：笔记经 macOS `tar`
搬运后带上 `._*.md`（AppleDouble），校验器抛 `UnicodeDecodeError` traceback。
已修（跳过 `._*` + 解码失败说人话），四方向真跑验过。

---

# 第三步：盲测（2026-08-08）

**为什么要做**：第二次重跑产出的手册 `validate.py` **满分**，
而里面一条关键命令抄反了。所以问题不是「校验器没跑」，是
**校验器的判据里没有「语义保真」这一项，也不可能有**。

## 做成了脚本，不是一次性手工操作

```bash
python3 scripts/blind_prep.py "<笔记目录>" /tmp/blind
```

只复制 `PLAYBOOK.md`，**移除截图嵌入与图注**，打印标准提示词。
自证：隔离目录里只有一个文件、`assets/transcript` 字样 0 次、关键命令仍在。

> **一个能偷看的盲测，测的是偷看能力。**

## 先考仪器：把已知错误种回去

我已经修好了那条 Triton 命令，所以**先造一份种回错误的副本**，
看盲测能不能看见它。

**能** —— 它在「可疑」第 8 条写：「`<3.7` 的上限像是编的，
既然承认命令是搜出来的，这个边界没有任何依据说明；
写死一个上限反而可能装到与 torch 不匹配的旧版。」

## 然后它报出 21 条缺信息 + 18 条可疑，我逐条复核了要紧的几条

| 盲测报出 | 复核 |
|---|---|
| 步骤 12/13 区间重叠，拼不成时间线 | ✅ 真的重叠 **124s** |
| 「根目录**不是** python_embeded」与步骤 7/8/9 互相打脸 | ✅ 第 82 行警告「不要学」，第 125 行让你进那个目录 |
| 装 xformers 再卸载会替换 torch，而全套价值建立在自带 torch 上 | ✅ 真实风险，手册零提示 |
| 残留一个空代码块，且在 batch 块里用 `#` 当注释 | ✅ **是我修笔记时留下的垃圾** |
| 第二次是否申报降级 | ✅ 申报了 2 条（第一次是 `degradations: []`） |
| 结论：**不能**独立完成，硬停在步骤 11 | 缺 workflow 来源 / 模型文件名 / ComfyUI 访问地址 |

## 一个更难受的发现：有帧 ≠ 读过帧

它申报的降级原文：

> 「证据帧来自 ffmpeg 抽帧（720p），未走 watch-skill OCR 流程；
>  **Hermes 无原生图片读取能力**」
> 「部分命令讲者说「输入下列指令」但字幕中未读出，**已从搜索引擎补全**」

`-z` 的 session 不落库，**所以我拿不到工具调用记录，不能断言它读没读图**。
但证据指向「抽了帧、没读帧」：那条命令错得恰好是**帧里有正确答案**。

> **而我新加的「证据帧非空」检查，分辨不出「有帧」和「读过帧」。**
> 它只能证明文件存在。

⇒ 所以 SKILL.md 那条「**画面原文以引用块留在紧邻位置**」不只是防抄错 ——
**它同时是「我真的读了这一帧」的可核对凭据**。

## 落成的检查（盲测发现 → 机械判定）

| 检查 | 级别 | 已验 |
|---|---|---|
| 步骤时间戳单调（起点不得倒退） | ERROR | 种入倒退 → 真报错 |
| 步骤区间不大幅重叠 | WARN | 抓到真实的 124s |

### 这里我又犯了一次同样的错，记下来

第一次加检查时我**选错了判据**：写的是「起点不得倒退」，
而实测数据 `[21:45–24:04]` → `[22:00–24:50]` 起点是**递增**的，
检查打了个 ✓。

> **我差点把那个 ✓ 当成「检查有效」** ——
> 而它只证明了「我要找的东西不在这个检查的视野里」。
> **先验证仪器能看见一个已知的东西** —— 这条我今天漏了第二次。

## 顺带修掉的悬空引用

质量控制里写着「可复现盲测：见阶段 2.4」，
而 **SKILL.md 里从来没有 2.4 这一节**（只有 2.1/2.2/2.3/2.5）——
一个被列为必做、却从未被定义的步骤。已补。
